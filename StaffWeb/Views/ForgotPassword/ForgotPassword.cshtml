@model string;
@{
}
@{
    ViewBag.Title = "Forgot Password";
}

<!DOCTYPE html>
<html>
	<head>
	<title></title>
</head>
 
<body>
	<div id="PlaceHolderHere"></div>
<h2>Forgot Password</h2>


 

	@*@using (Html.BeginForm("Forgotpassword","ForgotPassword",FormMethod.Post,new{@id="Form1"}))*@
	<form id="Form1" action="ResetPassword" method="post">
    <div class="form-horizontal">
        <hr />
        <div class="text-success">
            @TempData["msg"]
        </div>

				
				<div class="form-group">
            <label class="control-label col-md-2">Email</label>
          
		   <input class="form-control" id="Username" name="Username" type="text"  required>

		   
        </div>
		
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Reset Password" class="btn btn-success" onclick="submit() " data-toggle="ajax-modal"/>
				@*<input type="button" class="btn btn-primary" data-toggle="ajax-modal" data-target="#Reset" data-url="@Url.Action("Resetpassword")>Reset Password</button>*@
"
            </div>
        </div>
    </div>
	</form>



	

	@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
<script type="text/javascript" src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>




	@*<script type="text/javascript">
    function submit() {
        var data = $("#Reset").serialize();
        console.log(data);
        alert(data);
        $.ajax({
            type: 'POST',
            url: 'https://localhost:44366/ForgotPassword/Index',
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8', // when we use .serialize() this generates the data in query string format. this needs the default contentType (default content type is: contentType: 'application/x-www-form-urlencoded; charset=UTF-8') so it is optional, you can remove it
            data: data,
            success: function (Resetpassword) {
                alert('Successfully received Data ');
                console.log(result);
            },
            error: function () {
                alert('Failed to receive the Data');
                console.log('Failed ');
            }
        })
    }

</script>*@
<script>

$(function (){
	var PlaceHolderHere=$('#PlaceHolderHere')
	$('button[data-toggle="ajax-modal"]').click(function (event){
		var url = $ (this).data('url');
		$.get(url).done(funtion(data)){
			PlaceHolderElement.html(data);
			PlaceHolderElement.find('.modal').modal("show");

		}
	})
})

</script>

 <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
    <script type="text/javascript">
        $("body").on("submit", "#Form1", function () {
            return confirm("Do you want to submit?");
        });
    </script>
    @if (ViewBag.Message != null)
    {
        <script type="text/javascript">
            window.onload = function () {
                alert('@ViewBag.Message');
            };
        </script>
    }





</body>
</html>